# Dependencies
node_modules/
package-lock.json

# Runtime / local
.wrangler/
.dev.vars
dev_server.log
wrangler*.log
.DS_Store
.env
apps/selfhost/.env

# Build output
dist/
apps/*/dist

# Local wrangler state / secrets only (wrangler.jsonc is committed for CI)
# Worker secrets (TURNSTILE_SECRET_KEY, COOKIE_SECRET) live in CF via `wrangler secret`

.agent
